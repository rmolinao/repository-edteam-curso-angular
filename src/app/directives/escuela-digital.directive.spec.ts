import { EscuelaDigitalDirective } from './escuela-digital.directive';

describe('EscuelaDigitalDirective', () => {
  it('should create an instance', () => {
    const directive = new EscuelaDigitalDirective();
    expect(directive).toBeTruthy();
  });
});
